Int64Encoding
=============

Encode int64 value using zigzag and compress prefix byte which equals to zero

Usage:
  Encode: Int64EncodingUtils.encode(long n)
  Decode: Int64EncodingUtils.decode(byte[] val)
